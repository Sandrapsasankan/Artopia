import 'package:flutter/material.dart';
class compmsg extends StatelessWidget {
  const compmsg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(

      body: Center(
          child:Text("submitted successfully")
      ),
    );
  }
}
